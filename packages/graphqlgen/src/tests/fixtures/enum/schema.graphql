type User {
  id: ID!
  name: String!
  color: Color!
  role: Role!
  permissions: Permissions!
}

type Permissions {
  isAdmin: Boolean
}

enum Role {
  EDITOR
  COLLABORATOR
}

enum Color {
  RED
  GREEN
  BLUE
}

type Query {
  createUser(name: String!, type: Role!): User
}
